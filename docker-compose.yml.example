services:
  bot-terminal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot-terminal
    restart: unless-stopped
    env_file:
      - .env
    environment:
      BOT_WORKDIR: /workspace
    volumes:
      - ./:/workspace
